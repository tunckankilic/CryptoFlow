name: alerts
description: Price alerts for CryptoFlow

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Local packages
  core:
    path: ../core
  design_system:
    path: ../design_system
  market:
    path: ../market
  
  # State management
  flutter_bloc: ^8.1.0
  
  # Local storage
  hive_flutter: ^1.1.0
  
  # Utils
  equatable: ^2.0.5
  dartz: ^0.10.1

dev_dependencies:
  hive_generator: ^2.0.0
  build_runner: ^2.4.0
