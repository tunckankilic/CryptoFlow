name: portfolio
description: Portfolio management for CryptoFlow

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Local packages
  core:
    path: ../core
  design_system:
    path: ../design_system
  market:
    path: ../market
  
  # State management
  flutter_bloc: ^8.1.0
  
  # Local storage
  drift: ^2.13.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.8.3
  
  # Utils
  equatable: ^2.0.5
  dartz: ^0.10.1

dev_dependencies:
  drift_dev: ^2.13.0
  build_runner: ^2.4.0
